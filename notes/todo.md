
- On <http://localhost:4000/admin/courses/6/edit>, archived checkbox should be
a toggle button.
- On the new API key form, the subtitle / form description should just say
"remember: don't share / leak API keys".
- On <http://localhost:4000/courses/9/join_reqs/new> the course staff checkbox
should be a toggle button.
- There should be an "allow staff join requests" toggle button on the course
create / edit page, default false, which should show/hide the toggle button on
join requests; join requests should only be created with the staff flag set
if allowed.

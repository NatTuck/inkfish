

# Data Schema for Docker Containers

In DB:

 - docker_tags
   - name: string
   - dockerfile: text
   - build operation: Write file, run docker build -t ... .

From Docker API:

 - Images
 - Containers

